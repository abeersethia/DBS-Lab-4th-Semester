ALTER TABLE EMP
ADD REPORTS_TO CONSTRAINT FK_REPORTS_TO_EMPCODE REFERENCES EMP(EMPCODE);

ALTER TABLE SKILL
ADD CONSTRAINT STARTSWITH_S CHECK(SKILLID LIKE 'S%');

ALTER TABLE EMP_SKILL
ADD CONSTRAINT PK_EMPNO_SKILLID PRIMARY KEY(EMPNO,SKILLID);

ALTER TABLE PRJ_DETAILS
ADD CONSTRAINT UNQ_PRJ_NAME UNIQUE(PRJ_NAME);

ALTER TABLE CLIENT
ADD EMAIL VARCHAR(20) CONSTRAINT VALID_EMAIL CHECK(EMAIL LIKE '%@%.___');

ALTER TABLE EMP
ADD CONSTRAINT UNQ_PHONE UNIQUE(PHONE);

ALTER TABLE EMP
ADD CONSTRAINT UNQ_EMAIL UNIQUE(EMAIL);

ALTER TABLE MEDICAL_POLICY
DROP PK_MEDICAL_POLICY;

ALTER TABLE MEDICAL_POLICY
ADD PRIMARY KEY(EMPNO,POLICYNO,POLICYDATE)

ALTER TABLE MEDICAL_POLICY
DROP CONSTRAINT SYS_C0075535;

ALTER TABLE MEDICAL_POLICY
ADD CONSTRAINT MORE_THAN_TH CHECK(PREMIUM>1000);


